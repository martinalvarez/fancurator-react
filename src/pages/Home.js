import Layout from '../components/layout/Layout';
import PhotoCarousel from '../components/PhotoCarousel';

function Home() {
    return (
        <Layout>
            <PhotoCarousel />
        </Layout>
    );
}

export default Home;